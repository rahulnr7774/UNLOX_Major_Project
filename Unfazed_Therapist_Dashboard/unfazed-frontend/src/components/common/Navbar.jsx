import { Bell, Menu, Search, X } from 'lucide-react'
import { useState } from 'react'

export default function Navbar({ activeView, navItems, onNavigate }) {
  const [open, setOpen] = useState(false)
  return <header className="border-b border-ink/10 bg-cream/90 px-5 py-4 backdrop-blur sm:px-8 lg:px-12"><div className="mx-auto flex max-w-[1440px] items-center justify-between gap-6"><button className="flex items-center gap-3" onClick={() => onNavigate('Overview')}><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-moss font-display text-lg font-bold text-white">u</span><span className="font-display text-xl font-semibold tracking-[-.04em]">unfazed</span></button><nav className="hidden items-center gap-1 lg:flex">{navItems.map((item) => <button key={item} onClick={() => onNavigate(item)} className={`rounded-full px-4 py-2 text-sm font-semibold transition ${activeView === item ? 'bg-white text-ink shadow-sm' : 'text-ink/50 hover:text-ink'}`}>{item}</button>)}</nav><div className="flex items-center gap-2"><button aria-label="Search" className="hidden rounded-full p-2.5 text-ink/55 hover:bg-white sm:block"><Search size={18} /></button><button aria-label="Notifications" className="rounded-full p-2.5 text-ink/55 hover:bg-white"><Bell size={18} /></button><div className="ml-1 hidden h-9 w-9 items-center justify-center rounded-full bg-coral font-bold text-white sm:flex">M</div><button aria-label="Menu" className="rounded-full p-2 lg:hidden" onClick={() => setOpen(!open)}>{open ? <X size={20} /> : <Menu size={20} />}</button></div></div>{open && <nav className="mx-auto mt-4 flex max-w-[1440px] flex-col gap-1 border-t border-ink/10 pt-3 lg:hidden">{navItems.map((item) => <button key={item} onClick={() => { onNavigate(item); setOpen(false) }} className={`rounded-xl px-4 py-3 text-left text-sm font-semibold ${activeView === item ? 'bg-white text-ink' : 'text-ink/55'}`}>{item}</button>)}</nav>}</header>
}