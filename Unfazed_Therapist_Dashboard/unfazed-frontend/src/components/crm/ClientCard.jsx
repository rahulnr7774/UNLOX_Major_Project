export default function ClientCard({ client }) {
  return <div className="flex items-center gap-3 rounded-2xl p-3 transition hover:bg-white/70"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-sage text-sm font-bold text-moss">{client.name?.split(' ').map((word) => word[0]).join('').slice(0, 2).toUpperCase()}</div><div className="flex-1"><p className="text-sm font-semibold">{client.name}</p><p className="mt-0.5 text-xs text-ink/50">{client.last_session ? `Last session ${new Date(client.last_session).toLocaleDateString()}` : 'No sessions yet'}</p></div><span className="text-xs font-bold capitalize text-moss">{client.status || 'active'}</span></div>
}